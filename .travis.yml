language: cpp
compiler: g++

os: linux
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-7

script:
  - export CC=gcc-7
  - export CXX=g++-7
  - ./install-dependencies.sh 
  - make clean
  - make all -j4 
  - make deploy
